fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
SQLAlchemy==2.0.36
alembic==1.14.0
# psycopg 3 binary wheel — no C build needed on any wheel-supported
# platform. Only pulled in when TREASURY_DATABASE_URL starts with
# postgresql://; SQLite deployments never touch it.
psycopg[binary]==3.2.3
httpx==0.28.1
pytest==8.3.4
groq==0.13.0
